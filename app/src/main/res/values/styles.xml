<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="FullscreenTheme" parent="android:Theme.DeviceDefault.Light.NoActionBar.Fullscreen">
    </style>

    <style name="chessboard_theme" parent="@android:style/Theme.Holo.NoActionBar">
        <!--系统风格好看的,不要在这里纠结了-->
      <!-- 太难看了，还是用系统风格吧。。。 <item name="android:buttonStyle">@style/button_1</item>-->
    </style>

    <style name="button_1" parent="@android:style/Theme.Holo.NoActionBar">
        <item name="android:background">@drawable/button_1</item>
        <item name="android:layout_alignParentTop">true</item>
        <item name="android:layout_marginTop">20dp</item>
    </style>
    <!-- Backward-compatible version of ?android:attr/buttonBarStyle -->
    <style name="ButtonBar">
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingRight">2dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="android:background">@android:drawable/bottom_bar</item>
    </style>

    <!-- Backward-compatible version of ?android:attr/buttonBarButtonStyle -->
    <style name="ButtonBarButton" />

</resources>
